# 監査報告（2026-09-20、コミット 4b4cc8d / 618c913 反映）

対象はローカルの Python シミュレータ、学習、評価、保存済み海洋データの読込、
ブラウザ操縦コックピット、QGroundControl / MAVProxy 向け MAVLink v2 UDP SITL、
空間モードの強化学習です。実機、外部通信、モデル係数の実測校正は対象外です。

## 修正内容の要約

| 重要度 | 領域 | 修正 |
|---|---|---|
| 高 | PPO / 環境 | 行動は `(2,)`、確率・価値はスカラーに統一。PPO 確率比・価値・エントロピー勾配を修正し、勾配ノルム制限を追加 |
| 高 | ベクトル学習 | 行動前の状態と対応する行動・価値を保存。終了済み環境の進行を停止 |
| 高 | 海況スイープ | 波高・周期・試行数を環境設定に反映 |
| 高 | MAVLink 風評価 | `step(action=...)` で積分前に RL 行動を適用 |
| 高 | 故障・非武装 | 推力停止、故障機の再武装拒否 |
| 中 | 物理モデル | シード付きリセットで文字列化破壊を修正、浮力平衡に再初期化。静止時の船体抵抗と方向を修正。連続水没をイベント計数に変更 |
| 中 | 訓練ループ | 乱数シードを重み初期化にも使用。時間切れを高度だけで成功扱いにしない。波面と状態の評価時刻を一致 |
| 中 | コマンド | サーボ・速度変更を明示処理、未対応コマンドを拒否 |
| 中 | データ | フォールバック・無波成分扱い、モジュール相対の既定パス。リアルタイム波高の風浪追加で合成波高が増幅しないよう配分 |
| 中 | 検証 | 物理検証を現行コードで再計算するよう変更 |
| 中 | 着水評価 | 故障または評価時間まで継続、平均海面 0 横断のみでの停止を廃止 |
| 高 | 空間モード | バンク角を訓練環境と一致させて反映。ラダー出力の方向・サーボマップを 4 軸で拡張 |
| 高 | ベクトル環境 | 空間モードで `action_dim` から期待形状を導出 |
| 高 | 海況スイープ | 着水の 15 秒ホライズンを 30 秒（シナリオ別既定）に修正 |
| 高 | 抵抗曲線 | ハンプ／プレーニング境界の不連続ステップを滑らか補間に修正 |
| 中 | MWD 変換 | NDBC MWD → 内部 θ の座標変換を修正（テストも同期） |
| 中 | V_cruise 注釈 | 「L/D max × √3」の誤った根拠記述を修正 |
| 中 | advisor 既定ピッチ | 範囲を -8..15° に拡張 |
| 高 | 操縦統合 | `operator_training/`: ブラウザコックピット（HTTP+WebSocket）、自前 MAVLink v1/v2 コーデック、UDP SITL。QGroundControl / MAVProxy から Arm → Takeoff / Land / MANUAL_CONTROL が動作 |
| 高 | ロバスト学習 | 空間モードの教師にバンク・ラダーを追加。`train_robust.py` で行動空間 MSE + PPO、エピソード毎ランダム海況・風況、検証/評価シード分離、選択モデル保存 |

## 追加した検証機能

`python3 audit.py` で回帰テストと物理検証を実行し、終了コード、出力、時刻、
Python バージョン、ソース SHA-256 を `results/audit.json` に保存します。
失敗時は非ゼロ終了コードになるため、自動チェックにも利用できます。
依存パッケージは `requirements.txt` に記載しました。

操縦統合・MAVLink SITL は次の回帰テストで守られています：

```bash
python3 -m unittest discover -s tests -p 'test_operator_*.py'   # 148 件
node tests/test_operator_cockpit.cjs                              # 6 件
```

## 実行した確認

- 回帰テスト 265 件：通過。方策勾配の数値微分比較、PPO クリッピング、
  シード再現性、終了環境固定、海況・試行数反映、故障時推力、データ読込、
  空間モード、ベクトル環境、MAVLink v2 コーデック、UDP SITL、ブラウザ
  コックピット、座標変換、ロバスト学習の条件再現性・収束性。
- 単一・複数環境の短期学習：通過。学習損失・重みが有限値。
- 現行コードによる物理検証：通過。離水 3.78 秒 / x=18.0 m / Vx=7.55 m/s、
  トップスピード 18.34 m/s、離水時推力 492 N に対し抗力 21 N、
  着水 24.19 秒 / x=347.9 m / Vx=14.74 m/s / |Vz|=1.19 m/s、
  停止 40.00 秒、ピーク水面反力 2,681 N (≈ 3.3 倍重量)。
  静的推力 549 N は重量 824 N 未満であり、離水は浮力支持下で
  加速し失速速度を超えるモデル。
- `evaluate.run_takeoff()` / `run_landing()`：一時出力先で完走。
  6 枚の 3D 図、2 枚のプロファイル、CSV 出力まで例外なし。
- ブラウザコックピット: `python3 -m operator_training serve --port 0` で
  HTTP+WS が起動、`/cockpit/` が静的配信され `Start Manual` / `Start Takeoff`
  / `Take Control` / `Pause` / `Resume` の WS メッセージが受理される。
- MAVLink SITL: `python3 -m operator_training mavlink --duration 0.3`
  で正常終了し、`udp:127.0.0.1:14551 -> 14550` で HEARTBEAT が送出。
  `COMMAND_LONG` で ARM / TAKEOFF、`MANUAL_CONTROL` で PWM サーボが
  反映されることを結合テストで確認。
- ロバスト 4 軸訓練（v1/v2/pilot）：方向分散波・高度依存風を持つ空間
  環境で教師模倣＋ PPO を実行。`report.json` に条件分布・成功率・
  教師比較を保存、`{tag}_selected_policy.npz` を検証用シードで選択。
  訓練・検証・比較評価シードの重複はエラーで弾く。
- Ollama phi3.5 統合（追補、後続）：`fly_ollama.py --check` を含むスモーク。

## 残る制約

- `results/` の既存モデル・図・CSV は監査前・学習ループごとに再生成される
  性質上、再実行で内容が変わる。旧成功率は固定シードの条件下でのみ比較可能。
- 浮力・抗力・損傷係数は簡易モデルです。特に RL/MAVLink 経路の 0.05 秒刻み、
  接水判定に対する刻み幅依存性、波面相対速度、回転運動、電池消費は追加検証が
  必要です。
- 学習の時間上限は有限長エピソードの終端として扱います。無限時間問題の
  time-limit bootstrap は未実装です。ベクトル環境は逐次処理です。
- MAVLink 実装は自前コーデック（pymavlink は Python 3.14 でビルド不可）です。
  `mavlink_if.py` はプロセス内 API、UDP ワイヤは
  `operator_training/mavlink_wire.py` + `mavlink_udp.py`。
  QGroundControl / MAVProxy / Mission Planner から接続できますが、PX4 /
  ArduPilot ファームウェアそのものではなく、KMT 力学へのブリッジです。
  フィールド単位・座標規約を含む PX4/ArduPilot との完全な意味論的互換は
  保証していません。
- NOAA データは同梱スナップショットです。データの最新性や地点ラベルの正確性を
  オンラインで再確認していません。評価の "latest" はファイル内の先頭有効行を指します。
- ロバスト 4 軸訓練の条件分布（Hs×0.5..1.5、Tp×0.8..1.2、波向±45°、
  水平風±3 m/s、突風 0..0.8 m/s）は訓練用に合成したものであり、実測した
  気象分布ではありません。力学は既存の簡略モデルのままで、実機性能の保証や
  実測値による校正を行ったものではありません。
- 本結果から実機の運用限界や安全性は判定できません。
