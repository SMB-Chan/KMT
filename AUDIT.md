# 監査報告（2026-09-20）

対象はローカルのPythonシミュレータ、学習、評価、保存済み海洋データの読込です。
実機、外部通信、モデル係数の実測校正は対象外です。
Gitメタデータは利用できなかったため、コミットは作成していません。
変更前のPythonファイルとREADMEは `/tmp/hama-audit-original/` に一時保存しました。

## 修正内容

| 重要度 | 問題 | 修正 |
|---|---|---|
| 高 | 単一状態の `act()` がバッチ配列を返し、訓練環境と不整合 | 行動は `(2,)`、確率・価値はスカラーに統一 |
| 高 | PPOの確率比が常に1、価値・エントロピーの勾配が損失と不一致 | 行動確率の再計算、クリッピング条件の微分、係数の修正、全勾配のノルム制限 |
| 高 | ベクトル学習が行動後の状態を保存 | 行動前の状態と対応する行動・価値を保存 |
| 高 | 終了済み環境を進め続け、終了後の報酬・成否を集計 | 終了状態を固定し、軌跡への追加を停止 |
| 高 | 海況スイープの波高・周期・試行数が未反映 | 環境設定へ反映、指定したシード数だけ試行 |
| 高 | MAVLink風評価のRL行動が次のステップで制御器に上書きされる | `step(action=...)` で積分前に適用、訓練環境と同じ観測を使用 |
| 高 | 故障・非武装でも推力が発生、故障後も再武装可能 | 推力停止、故障機の再武装拒否 |
| 中 | シード付きリセットが海面オブジェクトを文字列に破壊 | パラメータを保った再構築、浮力平衡位置に初期化 |
| 中 | 乱数シードが重み初期化に使われない | 同じGeneratorを重みと行動サンプリングに使用 |
| 中 | 時間切れを高度だけで成功扱い、速い着水で終了しない | 厳密な離水目標を維持、全着水で成否判定 |
| 中 | 波面と状態の評価時刻が不一致 | ステップ後の位置・時刻で波面を再評価 |
| 中 | 静止時にも船体抵抗、後退時の抵抗方向が逆 | 抵抗を速さから求め、速度に逆向きに適用 |
| 中 | 連続水没の各ステップを別の衝突として計数 | 水中への進入をイベントとして計数 |
| 中 | 空中でも船首飛沫による浸水 | 船体接水を条件に追加 |
| 中 | サーボコマンドが実行されない | 明示的なサーボ処理、速度変更処理、未対応コマンドの拒否 |
| 中 | 空の海洋データ、ゼロ成分周期、CWD依存の読込 | フォールバック、無波成分の扱い、モジュール相対の既定パス |
| 中 | リアルタイム波高への風浪追加で合成波高が増幅 | 成分の二乗和が測定波高を保つよう配分 |
| 中 | 物理検証が古いCSVだけを確認 | 現行コードで離水・着水を再計算して検証 |
| 中 | 着水評価が平均海面高度0を横切るだけで停止 | 故障または評価時間まで継続 |

## 追加した検証機能

`python3 audit.py` で回帰テストと物理検証を実行し、終了コード、出力、時刻、
Pythonバージョン、ソースSHA-256を `results/audit.json` に保存します。
失敗時は非ゼロ終了コードになるため、自動チェックにも利用できます。
依存パッケージは `requirements.txt` に記載しました。

## 実行した確認

- 回帰テスト15件：通過。方策勾配の数値微分比較、PPOクリッピング、
  シード再現性、終了環境固定、海況・試行数反映、故障時推力、データ読込など。
- 単一・複数環境の短期学習：通過。学習損失・重みが有限値。
- 現行コードによる物理検証：通過。離水3.89秒、着水24.78秒、
  着水時降下速度約1.18 m/s、ピーク水面反力約2.8倍重量。
- `evaluate.run_takeoff()` / `run_landing()`：一時出力先で完走。
  6枚の3D図、2枚のプロファイル、CSV出力まで例外なし。軌跡は全て有限値。
  離水12秒時点は高度0.86 m・速度2.60 m/sで目標未達。
  着水30秒時点は高度0.76 m・速度0.11 m/s。双方で故障フラグなし。
  これはスクリプト制御器の結果であり、学習済み方策の性能ではありません。
- RL海況スイープと損傷付き評価：小規模な条件で実行経路を確認。
  少数シード・未学習方策のため、成功率の性能主張には使っていません。

## 残る制約と旧成果物の扱い

- `results/` の既存モデル・図・CSVは監査前のものとして保持しました。
  学習、成功判定、海況設定の修正があるため、旧成功率は再利用できません。
  本監査では長時間の本学習や全海況の再評価までは実施していません。
- 浮力・抗力・損傷係数は簡易モデルです。特にRL/MAVLink風経路の0.05秒刻み、
  接水判定に対する刻み幅依存性、波面相対速度、回転運動、電池消費は追加検証が必要です。
- 学習の時間上限は有限長エピソードの終端として扱います。無限時間問題の
  time-limit bootstrapは未実装です。ベクトル環境は逐次処理です。
- MAVLink風インターフェースはプロセス内の模擬APIです。ワイヤ形式、ACK、
  フィールド単位・座標規約を含むPX4/ArduPilotとの適合性は保証していません。
- NOAAデータは同梱スナップショットです。データの最新性や地点ラベルの正確性を
  オンラインで再確認していません。評価の “latest” はファイル内の先頭有効行を指します。
- 本結果から実機の運用限界や安全性は判定できません。


## 追補：Ollama phi3.5統合（2026-09-20）

- `ollama_pilot.py`：ローカルHTTP、構造化操縦出力、範囲検証、モデル確認。
- `fly_ollama.py`：離水・着水ランナー、同期推論、既存制御器フォールバック、JSONL記録。
- 新規テスト11件を追加。従来15件と合わせ26件、および再計算の物理検証が通過。
- 実際のローカル `phi3.5:latest` で離水開始3秒・着水進入1秒を確認。
  合計5判断／80物理ステップ、フォールバック0回。双方とも時間上限で終了。
  これらはAPIから物理積分までの結合確認であり、飛行成功率の評価ではありません。
- 実測時間は離水約28秒、着水約14秒。推論待ち中はシミュレーションを停止し、
  実機・実時間制御は実装していません。ソケットタイムアウトは全実行時間の上限ではありません。
- 記録：`results/ollama_phi35_smoke/`、`results/ollama_phi35_landing_smoke/`。
